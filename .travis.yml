sudo: false
language: node-js
before_install:
services:
  - redis-server
install:
  - npm install
  - npm install -g sails
before_script:
  - git clone http://github.com/gmod/jbserver
  - cd jbserver
  - npm install
  - npm install gmod/jblast
  - ./bin/blast_getBlast.js
  - ./bin/blast_downloadDb.js htgs.05
  - nohup sails lift > /dev/null 2>&1 &
  - cd ..
script:
  - npm test
after_failure:
